<%= render partial: "shared/notice" %>
<h1>New Task List</h1>

<%#= render 'form', task_list: @task_list %>
 <%= semantic_form_for @task_list do |f| %>
    <%= f.inputs :name, :user_id %>
    <%= f.actions :submit, :cancel %>
<% end %>

<%= link_to 'Back', task_lists_path %>
