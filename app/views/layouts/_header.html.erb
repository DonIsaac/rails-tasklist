
<header style="padding-bottom: 70px;">
  	<nav class="navbar navbar-default navbar-fixed-top">
    	<div class="container-fluid">
			<div class="navbar-header">
				<a href="/">
					<span class="navbar-brand">Taskr</span>
				</a>
			</div>
			<div class="collapse navbar-collapse">
				<ul class="nav navbar-nav navbar-right">
					<% if logged_in? %>
						<li>
							<!--a href="#">My Tasks</a-->
							<%= link_to 'My Tasks', task_lists_path %>
						</li>
						<li class="dropdown">
							<a href="#" data-toggle="dropdown">
								<%= current_user().firstname %> <span class="caret"></span>
							</a>
							<ul class="dropdown-menu">
								<li><a id="my-profile" href="#<%#=  %>">
									<span class="glyphicon glyphicon-user"></span> My Profile
									</a></li> 
								<li><a id="sign-out" data-href="<%= session_url %>">
									<span class="glyphicon glyphicon-off"></span> Sign Out
									</a></li>
							</ul>
						</li>
					<% else %>
						<li>
							<%= link_to 'Sign Up', new_user_path %>
        				</li>
        				<li>
							<%= link_to 'Sign In', new_session_path %>
						</li>
					<% end %>
        			
			</ul>
			</div>
		</div>
	</nav>
</header>
